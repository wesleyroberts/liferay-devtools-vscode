# __PROJECT_TITLE__

Client extension do tipo `customElement`.

## Como usar

1. Ajuste os arquivos em `assets/`
2. Execute `../../gradlew clean build` dentro do workspace
3. Faça o deploy do zip gerado em `dist/`
